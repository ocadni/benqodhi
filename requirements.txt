# Python dependencies for the BENQODHI repository.
#
# quarto-cli installs the actual Quarto command-line tool (the `quarto` command)
# used to render and preview the website in ./website. It downloads the matching
# Quarto binary from GitHub during installation, so an internet connection is
# required at install time.
#
# Pin exact versions and re-run `pip install -r requirements.txt` after bumping them.
# Current version verified on PyPI: https://pypi.org/project/quarto-cli/
quarto-cli==1.10.18
